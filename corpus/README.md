# Corpus — placeholder

Tools for capturing and analyzing your own public posts and reception data.

Coming soon:

- **`reddit-corpus-toolkit/`** — Python script + INDEX schema for capturing your Reddit threads (your own posts and admired threads) via the public `.json` endpoints, no auth, no automation, no ToS friction. Builds a local time-series of how each post was received so you can grade your own writing patterns over time.

Status: 🟡 not yet published. Used to capture my own corpus first — landing here once the capture-and-analysis workflow stabilizes.
