# Agents — placeholder

Reference patterns for small autonomous-agent fleets that produce verifiable work.

Coming soon:

- **`fleet-starter/`** — the OpenClaw-shaped 3-agent reference fleet pattern: one orchestrator, multiple workers, every action producing a Telegram receipt + a disk artifact + a real-world side effect. Every claim verifiable.
- **`closed-loop-bug-protocol/`** — the process kit for handling agent bugs: PR + RCA + monitor, every time. Templates and example RCAs.

Status: 🟡 not yet published. Working from real running fleets — these will land here once they are abstracted enough to be useful out of the box.
